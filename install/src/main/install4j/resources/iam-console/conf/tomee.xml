<?xml version="1.0" encoding="UTF-8"?>
<tomee>
	<!-- see http://tomee.apache.org/containers-and-resources.html -->

	<Deployments dir="apps" autoDeploy="true" />

	<TransactionManager id="myTransactionManager"
		type="TransactionManager">
		defaultTransactionTimeout = 60 minutes
		flushSleepTime = 50 Milliseconds
	</TransactionManager>

	<Resource id="jdbc/soffid" type="DataSource">
		accessToUnderlyingConnectionAllowed = false
		alternateUsernameAllowed = false
		connectionProperties =
		defaultAutoCommit = true
		defaultReadOnly =
		definition =
		ignoreDefaultValues = false
		initialSize = 0
		jtaManaged = true
		maxActive = 50
		maxIdle = 20
		maxOpenPreparedStatements = 0
		maxWaitTime = 30 seconds
		minEvictableIdleTime = 30 minutes
		minIdle = 0
		numTestsPerEvictionRun = 3
		poolPreparedStatements = false
		serviceId =
		testOnBorrow = true
		testOnReturn = false
		testWhileIdle = false
		timeBetweenEvictionRuns = -1 millisecond
		validationQuery = select 1
		jdbcDriver = com.soffid.iam.db.SoffidBrokerDriver
	    jdbcUrl = jdbc:soffid:db
	</Resource>
	<Container id="myStatelessContainer" type="STATELESS">
		accessTimeout = 30 seconds
		callbackThreads = 5
		closeTimeout = 5 minutes
		garbageCollection = false
		idleTimeout = 0 minutes
		maxAge = 0 hours
		maxAgeOffset = -1
		maxSize = 250
		minSize = 20
		replaceAged = true
		replaceFlushed = false
		strictPooling = true
		sweepInterval = 5 minutes
	</Container>
</tomee>
