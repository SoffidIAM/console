<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>

	<datanode name="serveis">		
		<finder name="servei" type="servei">
			<ejb-finder jndi="java:comp/env/ejb/ServeiEJB"
				method="findServeisByCriteri" if = "${queryEnabled}">  <!-- service: query -->
				<parameter value="${codi}"/>
				<parameter value="${descripcio}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.Servei">
			</new-instance-bean>
		</finder>
	</datanode>
	
	<datanode name="servei">
		<ejb-handler jndi="java:comp/env/ejb/ServeiEJB">
			<insert-method method="create" returnBean="true">
				<parameter value="${instance}" />
			</insert-method>
			<delete-method method="delete">
				<parameter value="${instance}" />
			</delete-method>
			<update-method method="update">
				<parameter value="${instance}" />
			</update-method>
		</ejb-handler>
		<validator>
			<attribute-validator expr="${instance.codi}" notNull="true" friendlyName="service.codeValidation"/>
		</validator>
	</datanode>
	
</zkib-model>
