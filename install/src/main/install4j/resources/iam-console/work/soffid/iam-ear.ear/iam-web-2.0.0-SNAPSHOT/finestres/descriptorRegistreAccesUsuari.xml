<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>
	<datanode name="registresAcces">
		<!--finder name="registreAcces" type="registreAcces">
			<ejb-finder jndi="java:comp/env/ejb/RegistreAccesEJB"
				method="findRegistreByDataIniAndCodiUsuari"  if = "${queryEnabled}"> 
				<parameter value="${data}"/>
				<parameter value="${usuari}"/>
				<parameter value="${numRegistres}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.RegistreAcces">
			</new-instance-bean>			
		</finder-->
		<finder name="registreAcces" type="registreAcces">
			<ejb-finder jndi="java:comp/env/ejb/RegistreAccesEJB"
				method="findDarrersRegistresByCodiUsuari"  if = "${queryEnabled}"> <!-- user:accessRegister:query -->
				<parameter value="${usuari}"/>
				<parameter value="${numRegistres}"/>
				<parameter value="sso"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.RegistreAcces">
			</new-instance-bean>			
		</finder>		
	</datanode>
	
	<datanode name="registreAcces">
	</datanode>
	
</zkib-model>
