<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>

	<datanode name="dominisCorreu">		
		<finder name="dominiCorreu" type="dominiCorreu">  <!-- mail:query, user:create, user:update -->
			<ejb-finder jndi="java:comp/env/ejb/LlistesDeCorreuEJB"
				method="findDominisCorreuByFiltre" if = "${queryEnabled}">
				<parameter value="${codi}"/>
				<parameter value="${descripcio}"/>
				<parameter value="${obsolet}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.DominiCorreu">
			</new-instance-bean>
		</finder>
	</datanode>	
	
	<datanode name="dominiCorreu">
		<ejb-handler jndi="java:comp/env/ejb/LlistesDeCorreuEJB">
			<insert-method method="create" returnBean="true"> <!-- mail:create -->
				<parameter value="${instance}" />
			</insert-method>
			<delete-method method="delete">  <!-- mail:delete -->
				<parameter value="${instance}" />
			</delete-method>
			<update-method method="update">  <!-- mail:update -->
				<parameter value="${instance}" />
			</update-method>
		</ejb-handler>
		<validator>
			<attribute-validator expr="${instance.codi}" notNull="true"
				friendlyName="mailDomain.CodeValidation"/>
		</validator>
	</datanode>
</zkib-model>
