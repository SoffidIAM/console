<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>
	<!--datanode name="registresAcces">
		<finder name="registreAcces" type="registreAcces">
			<ejb-finder jndi="java:comp/env/ejb/RegistreAccesEJB"
				method="findRegistresAccesByFiltre"  if = "${queryEnabled}">  //accessRegister:query  
				<parameter value="${data}"/>
				<parameter value="${dataFi}"/>
				<parameter value="${servidor}"/>
				<parameter value="${client}"/>
				<parameter value="${usuari}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.RegistreAcces">
			</new-instance-bean>
		</finder>
	</datanode-->
	
	<datanode name="registresAcces">
		<finder name="registreAcces" type="registreAcces">
			<ejb-finder jndi="java:comp/env/ejb/RegistreAccesEJB"
				method="findRegistresAccesByFiltreNou"  if = "${queryEnabled}">  <!-- accessRegister:query --> 
				<parameter value="${data}"/>
				<parameter value="${dataFi}"/>
				<parameter value="${servidor}"/>
				<parameter value="${client}"/>
				<parameter value="${usuari}"/>
			</ejb-finder>
			<new-instance-bean className="es.caib.seycon.ng.comu.RegistreAcces" />
		</finder>
	</datanode>
	
	<datanode name="registreAcces" />

</zkib-model>
