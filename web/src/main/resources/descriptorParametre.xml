<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>

	<datanode name="parametres">
		<finder name="parametre" type="parametre">
			<ejb-finder jndi="java:comp/env/ejb/ConfiguracioEJB"
				method="findConfiguracioByFiltre" if="${queryEnabled}"> <!-- parameter:query -->
				<parameter value="${codi}"/>
				<parameter value="${valor}"/>
				<parameter value="${descripcio}"/>
				<parameter value="${xarxa}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.Configuracio">
			</new-instance-bean>
		</finder>
	</datanode>	
	
	<datanode name="parametre">
		<ejb-handler jndi="java:comp/env/ejb/ConfiguracioEJB">
			<insert-method method="create" returnBean="true">
				<parameter value="${instance}" />
			</insert-method>
			<delete-method method="delete">
				<parameter value="${instance}" />
			</delete-method>
			<update-method method="update">
				<parameter value="${instance}" />
			</update-method>
		</ejb-handler>
		<validator>
			<attribute-validator expr="${instance.valor}" notNull="true" friendlyName="parametres.ValueValidation"/>
			<attribute-validator expr="${instance.codi}" notNull="true" friendlyName="parametres.CodiValidation"/>
		</validator>
	</datanode>
	
</zkib-model>
