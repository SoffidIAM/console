<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>
	<!--datanode name="registresAcces">
		<finder name="registreAcces" type="registreAcces">
			<ejb-finder jndi="java:comp/env/ejb/RegistreAccesEJB"
				method="findRegistresAccesByMaquinaAccesSSO"  if = "${queryEnabled}">
				<parameter value="${data}"/>
				<parameter value="${servidor}"/>
				<parameter value="${numRegistres}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.RegistreAcces">
			</new-instance-bean>			
		</finder>		
	</datanode-->
	
	<datanode name="registresAcces">
		<finder name="registreAcces" type="registreAcces">
			<ejb-finder jndi="java:comp/env/ejb/RegistreAccesEJB"
				method="findDarrersRegistresAccesMaquinaSSO"  if = "${queryEnabled}">
				<parameter value="${servidor}"/>
				<parameter value="${numRegistres}"/>
			</ejb-finder>
			<new-instance-bean
				className="es.caib.seycon.ng.comu.RegistreAcces">
			</new-instance-bean>			
		</finder>		
	</datanode>
	
	
	<datanode name="registreAcces">
	</datanode>
	
</zkib-model>
