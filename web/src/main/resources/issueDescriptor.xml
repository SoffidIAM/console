<?xml version="1.0" encoding="UTF-8"?>

<zkib-model>

	<datanode name="root">
		<finder name="issue" type="issue">
			<ejb-finder jndi="java:/module/IssueService-v2" method="findIssuesByJsonQueryAsync">
				<parameter value="${query}"/>
			</ejb-finder>
		</finder>
		<finder name="user" type="user">
			<ejb-finder jndi="java:/module/UserService-v2"
				method="findUserByJsonQuery" if = "${userquery != null}">
				<parameter value="${userquery}"/>
			</ejb-finder>
		</finder>
	</datanode>

	<datanode name="issue">
		<ejb-handler jndi="java:/module/IssueService-v2">
			<update-method method="update" returnBean="true">
				<parameter value="${instance}"/>
			</update-method>
		</ejb-handler>
	</datanode>

	<datanode name="user"> 
		<ejb-handler jndi="java:/module/UserService-v2">
			<insert-method method="create" returnBean="true" >  <!-- user:create -->
				<parameter value="${instance}"/>
			</insert-method>
			<delete-method method="delete">    
				<parameter value="${instance}" />
			</delete-method>
			<update-method method="update"> <!-- user:update -->
				<parameter value="${instance}" />
			</update-method>
		</ejb-handler>
	</datanode>
</zkib-model>
